// This page is no longer used — access is open to all authenticated users.
// Redirects to root so any lingering references don't break the app.
export default function PendingApprovalPage() {
  return null;
}
